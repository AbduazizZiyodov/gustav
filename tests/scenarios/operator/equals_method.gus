// Bound methods have identity equality.
class Foo {
  method() {}
}

var foo = Foo();
var fooMethod = foo.method;

// Same bound method.
stdout fooMethod == fooMethod; // expect: true

// Different closurizations.
stdout foo.method == foo.method; // expect: false

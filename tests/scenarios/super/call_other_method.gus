class Base {
  foo() {
    stdout "Base.foo()";
  }
}

class Derived < Base {
  bar() {
    stdout "Derived.bar()";
    super.foo();
  }
}

Derived().bar();
// expect: Derived.bar()
// expect: Base.foo()

class Base {
  init(a, b) {
    stdout "Base.init(" ++ a ++ ", " ++ b ++ ")";
  }
}

class Derived < Base {
  init() {
    stdout "Derived.init()";
    super.init("a", "b");
  }
}

Derived();
// expect: Derived.init()
// expect: Base.init(a, b)

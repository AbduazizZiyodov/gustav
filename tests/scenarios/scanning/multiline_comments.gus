/*
fun caller(g) {
  g();
  // g should be a function, not nil.
  stdout g == nil;
}

fun callCaller() {
  var capturedVar = "before";
  var a = "a";

  fun f() {
    capturedVar = "after";
    //return nil;
  }

  caller(f);
}

callCaller();

// HELICOPTER
*/

// expect: EOF  None
class Outer {
  method() {
    stdout this; // expect: Outer<Instance>

    fun f() {
      stdout this; // expect: Outer<Instance>

      class Inner {
        method() {
          stdout this; // expect: Inner<Instance>
        }
      }

      Inner().method();
    }
    f();
  }
}

Outer().method();

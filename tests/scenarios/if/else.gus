// Evaluate the 'else' expression if the condition is false.
if (true) stdout "good"; else stdout "bad"; // expect: good
if (false) stdout "bad"; else stdout "good"; // expect: good

// Allow block body.
if (false) nil; else { stdout "block"; } // expect: block

var TIMEOUT = 1;

var start = clock();
sleep(TIMEOUT);
var end = clock();
print TIMEOUT <= (end - start);

// expect: true

print sleep; // expect: <native fn>
print clock; // expect: <native fn>
print input; // expect: <native fn>
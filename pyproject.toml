[project]
name = "gustav"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=1.15.0",
    "pre-commit>=4.1.0",
    "pytest>=8.4.1",
    "rich>=14.0.0",
    "ruff>=0.11.12",
]
